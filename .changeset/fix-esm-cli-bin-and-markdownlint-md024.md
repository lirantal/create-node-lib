---
"create-node-lib": patch
---

Fix scaffold templates to build CLI binaries as ESM-only outputs (`dist/bin/cli.mjs`), update migration guidance for the split tsdown config layout, and allow Changesets changelog heading repetition with markdownlint MD024 sibling-only mode.
